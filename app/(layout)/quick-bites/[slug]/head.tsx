// Server component only (no "use client")
export default function Head() {
  // JSON-LD for this route is rendered directly inside page.tsx to align
  // with Next.js metadata handling. Keeping this file so existing imports
  // remain valid, but we intentionally render nothing here.
  return null;
}